const defaultQuotes = [
    'this is simple quote',
    'this is simple quote second',
    'this is simple quote third',
    'this is simple quote forth',
    'this is simple quote five'
];
window.onload = function () {
    main();
}

function main() {
    let newQuoteBtn = document.getElementById('new-quote-btn');
    let quoteBody = document.getElementById('quote-body');
}